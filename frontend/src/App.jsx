import HomePage from "./pages/HomePage.jsx";

/**
 * Root component. This small app has a single page, so App simply renders it.
 * If the project grew, routing (e.g. react-router) would live here.
 */
function App() {
  return <HomePage />;
}

export default App;
